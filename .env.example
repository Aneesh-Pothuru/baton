# Required by compose because the service binds to the container network.
BATON_API_TOKEN=replace-with-a-long-random-secret

# Host port only; the compose file keeps it on loopback by default.
BATON_PORT=8020
